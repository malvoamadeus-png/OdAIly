# Data Directory

- `config/`: local runtime configuration. Copy `market_brief.example.json` to
  `market_brief.json` and edit locally.
- `raw/`: raw Yahoo and Gate quote snapshots.
- `processed/`: generated brief run records for all publishing tasks.
- `exports/`: future exports.

Runtime data is ignored by Git.
