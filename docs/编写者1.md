# 编写者1

## 职责

编写者1对应 `x_processing` 的 `write` 阶段，负责根据当前任务类型选择 Prompt，并生成草稿标题与正文。

## Prompt 选择

- `regular` -> `x_regular_writer`
- `onchain` -> `x_onchain_writer`
- `funding` -> `x_funding_writer`
- `non_mainstream_media` -> `mainstream_media_writer`
- 历史兼容 `tasks.source = 'mainstream_media'` -> `mainstream_media_writer`

`non_mainstream_media_writer` 不再被新任务使用，只保留历史模板记录。

## 外媒写作输入

统一外媒写作输入现在分两类：

- 外媒全文：媒体名、作者、原标题、来源链接、正文
- 历史外媒快讯：媒体名、原标题、来源链接、原始快讯

CoinDesk、Cointelegraph、Decrypt 的新任务全部属于第一类。

## 模板种子

- 当前在用：`docs/主流外媒快讯模板.txt` -> `mainstream_media_writer`
- 历史保留：`docs/外媒模板.txt` -> `non_mainstream_media_writer`

## 相关文档

- `判断者.md`
- `编写者2.md`
- `控制台-Prompt.md`
