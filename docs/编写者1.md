# 编写者1

## 职责

编写者1对应 `x_processing` 的 `write` 阶段，负责根据当前任务类型选择 Prompt，并生成草稿标题与正文。

默认模型配置：

- `X_PROCESS_WRITER_MODEL`：默认 `gpt-5.5`
- `X_PROCESS_WRITER_REASONING_EFFORT`：默认 `medium`

Chrome 插件 `快速生成` 会复用编写者1 Prompt，但单独把文案生成模型固定为 `gpt-5.4-mini`，reasoning effort 固定为 `low`；这不会改变自动化主链路和插件 `生成快讯` 的默认 writer 配置。

## Prompt 选择

- `regular` -> `x_regular_writer`
- `onchain` -> `x_onchain_writer`
- `funding` -> `x_funding_writer`
- `non_mainstream_media` -> `mainstream_media_writer`
- `ai_source` -> `mainstream_media_writer`
- 历史兼容 `tasks.source = 'mainstream_media'` -> `mainstream_media_writer`

`non_mainstream_media_writer` 不再被新任务使用，只保留历史模板记录。

## 外媒写作输入

统一外媒写作输入现在分两类：

- 外媒全文：媒体名、作者、原标题、来源链接、正文
- AI信源全文：媒体名、作者、原标题、来源链接、正文，输入块标题显示为“待处理AI信源原文”
- 历史外媒快讯：媒体名、原标题、来源链接、原始快讯

CoinDesk、Cointelegraph、Decrypt 的新任务属于外媒全文；TheElec CHINA、ETNews Electronics、ETNews SW 属于 AI信源全文。

## 模板种子

- 当前在用：`docs/主流外媒快讯模板.txt` -> `mainstream_media_writer`
- 历史保留：`docs/外媒模板.txt` -> `non_mainstream_media_writer`

## 相关文档

- `判断者.md`
- `编写者2.md`
- `控制台-Prompt.md`
