# 判断者

## 职责

判断者指 `x_processing` 的 `judge` 阶段。

默认模型配置：

- `X_PROCESS_JUDGE_MODEL`：默认 `gpt-5.4-mini`
- `X_PROCESS_JUDGE_REASONING_EFFORT`：默认 `low`

它负责处理：

- `tasks.source = 'x'`
- `tasks.source in ('blockbeats', 'panews', 'jinse')`
- `tasks.source = 'non_mainstream_media'`
- `tasks.source = 'ai_source'`

## 外媒全文判断

统一外媒全文现在全部通过 `tasks.source = 'non_mainstream_media'` 进入判断者。

判断结果仍只允许：

- `route = 'non_mainstream_media'`
- `route = 'discard'`

这样可以保持现有流水线兼容，但后续写作模板已经统一改成 `mainstream_media_writer`。

## AI信源全文判断

AI信源全文通过 `tasks.source = 'ai_source'` 进入判断者，当前站点包括 TheElec CHINA、ETNews Electronics、ETNews SW、ZDNet Korea Semiconductor、CTEE Semiconductor。

当前 AI信源判断者是确定性直通，不调用判断模型，不按内容丢弃。

只要前置搜索阶段没有判重，判断者直接写入 `news_type = 'ai_source'`，并推进到后续写作链路。

## 丢弃规则

对 `X / blockbeats / panews / jinse` 这条判断链路，判断者模型只允许按输出结构丢弃以下内容：

- `pure_emotion`
- `baseless_trading_call`
- `daily_chatter`

系统不再在调用判断模型前，对“AI 相关但非 Crypto”的内容做确定性预丢弃；同时 `X / 竞品` 这条判断 prompt 也不再提供 `non_crypto_ai` 丢弃项。AI 相关内容应进入正常判断流程，由模型决定保留并路由到常规、链上或融资模板，或仅在明确属于情绪、喊单、闲聊时丢弃。

`ai_source` 不执行任何确定性预丢弃，也不执行模型丢弃。无论是否 Crypto、是否 AI 泛科技内容，都会通过判断者。

Chrome 插件 `快讯生成` 的文本生成接口不经过判断者；插件由编辑在前端手动选择 `常规 / 融资 / 链上` 模板。

## X 作者名输入

对 `tasks.source = 'x'` 的判断输入，判断者会优先使用统一作者名 `effective_author_name` 作为 prompt 里的作者字段。

- 优先使用当前 `x_capture_accounts` 里按账号维护的 `写作名`
- 没有 `写作名` 时回退到任务元数据里的原始 `author_display_name`
- 仍为空时回退到任务元数据里的 `author_username`

这样判断者与编写者1在 X 发言人名称上保持一致，不会出现前后口径不一致。

## 输出

- 保留：`tasks.status = 'judged'`，`x_task_pipeline.news_type = 'non_mainstream_media'`
- AI信源保留：`tasks.status = 'deduped'`，`x_task_pipeline.news_type = 'ai_source'`
- 丢弃：`tasks.status = 'discarded'`

竞品来源（`blockbeats` / `panews` / `jinse`）在判断阶段如果遇到 AI 网关空响应、非 JSON 或临时解析异常，会走保守兜底路由：

- 命中融资关键词时进入 `funding`
- 命中链上、安全、资金流、主流资产符号等关键词时进入 `onchain`
- 其他情况进入 `regular`

兜底只用于避免合格竞品快讯因为模型接口空 body 直接变成 `judge_failed`；AI 相关内容不再有确定性预丢弃。

## 相关文档

- `收集者-非主流媒体.md`
- `收集者-AI信源.md`
- `编写者1.md`
