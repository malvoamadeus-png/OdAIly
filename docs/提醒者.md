# 提醒者

## 职责

提醒者只负责外媒标题提醒链路的 Telegram 通知。

它不负责统一外媒编写链路的内部提醒；编写链路仍由 `x_processing` 在 `format_publish` 完成后发送通知。

## 输入

- `tasks.source = 'external_media_alert'`
- `tasks.status = 'deduped'`
- `metadata.site_display_name`
- `title`
- `source_url`

`mainstream_media` 只保留历史兼容，不再作为新的提醒入口。

## 输出

- 发送 Telegram 标题提醒
- 回写 `external_media_alert_pipeline.telegram_result`
- 更新 `tasks.status = 'notified'`

## 提醒文案

- 来源名优先使用站点显示名，例如 `The Block`
- 路线名固定由领域判断链路提供
- The Block 现在走这条链路，只提醒标题，不抓正文

## 相关文档

- `收集者-非主流媒体.md`
- `判断者-领域分类.md`
- `搜索者.md`
