# OdAIly 生产存储语境

本语境定义生产数据迁移、运行和灾难回切时使用的统一业务术语。

## Language

**活动后端**：
当前被全部 OdAIly 服务视为业务事实来源的数据库后端，同一时刻只能有一个。
_Avoid_: 主从、双主

**存储世代**：
一次连续运行期的唯一标识；任务只能由创建它的存储世代消费。
_Avoid_: 版本号、部署批次

**回切基线**：
SQLite 接管前 Supabase 中最后一份完整且可恢复的业务状态。
_Avoid_: 最新备份、实时副本

**SQLite 时期数据**：
回切基线之后仅进入 SQLite 的业务记录；灾难回切时允许不补入 Supabase。
_Avoid_: 增量备份

**去重账本**：
跨存储世代保留来源业务键的本地记录，用于阻止回切后再次采集或发布已经处理过的来源内容。
_Avoid_: 任务队列

**本地操作者**：
唯一获准访问 OdAIly 网页和插件的人类身份；迁移后身份、密码凭据与会话均由 Linux 本地服务持有。
_Avoid_: Supabase 用户、控制台管理员、插件用户

**旧系统身份**：
冻结在 Supabase 回切基线中的历史登录身份，仅供启动旧版本时使用，不属于新版本运行依赖。
_Avoid_: 本地操作者
